<!DOCTYPE html>
<html>

<head>
    <%- include('../partials/head'); %>
</head>

<body class="container bg-secondary" style="height: 100vh;">
    <div class="h-100 container d-flex justify-content-center align-items-center">
        <div class="w-50 p-5 pt-4 bg-primary-subtle" style="height: 33em; border-radius: 10px;">
            <div class="mb-2 d-flex justify-content-between align-items-center">
                <h3 class="mb-0">Form Tambah Data</h3>
                <a href="/about" style="text-decoration: none; color:black"><i class="bi bi-x-lg"
                        style="cursor: pointer;"></i></a>
            </div>
            <form action="/update/<%= data._id %>" method="post" class="h-100 d-flex flex-column">
                <div class="mb-3">
                    <label for="nama-depan" class="form-label fw-semibold">Nama Depan</label>
                    <input type="text" name="namaDepan" class="form-control" id="nama-depan"
                        placeholder="Masukkan Nama Depan" value="<%= data.namaDepan %>" required>
                </div>
                <div class="mb-3">
                    <label for="nama-belakang" class="form-label fw-semibold">Nama Belakang</label>
                    <input type="text" name="namaBelakang" class="form-control" id="nama-belakang"
                        placeholder="Masukkan Email" value="<%= data.namaBelakang %>" required>
                </div>
                <div class="mb-3">
                    <label for="email" class="form-label fw-semibold">Alamat Email</label>
                    <input type="email" name="email" class="form-control" id="email" placeholder="Masukkan Email"
                        value="<%= data.email %>">
                </div>
                <div class="mb-3">
                    <label for="saldo" class="form-label fw-semibold">Saldo</label>
                    <input type="text" name="saldo" class="form-control" id="saldo" placeholder="Masukkan Saldo"
                        value="<%= data.saldo %>">
                </div>
                <div class="d-flex mt-3 justify-content-between">
                    <div class="form-check d-flex align-items-center gap-2">
                        <input onclick="cekStatus()" class="form-check-input" name="status" type="checkbox"
                            id="flexCheckDefault" style="width: 22px; height: 22px;">
                        <label class="form-check-label" for="flexCheckDefault">
                            Tandai sebagai customer tidak aktif
                        </label>
                    </div>
                    <button type="submit" class="btn btn-primary">Ubah</button>
                </div>
            </form>
        </div>
    </div>

    <!-- modal component -->
    <div class="modal fade" id="modal" tabindex="-1" aria-labelledby="modal" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h1 class="modal-title fs-5" id="exampleModalLabel">Deaktif Customer</h1>
                    <button onclick="closeModal()" type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    Apakah anda yakin ingin menandai tidak aktif untuk customer ini?
                </div>
                <div class="modal-footer">
                    <button onclick="closeModal()" type="button" class="btn btn-secondary" data-bs-dismiss="modal">Tidak</button>
                    <button onclick="closeModal()" type="button" class="btn" data-bs-dismiss="modal"
                        style="background-color: darkred; color:#fff">Tandai</button>
                </div>
            </div>
        </div>
    </div>

    <!-- scripts -->
    <%- include('../partials/scripts'); %>
        <!-- scripts partial for bootstrap styling -->
        <script>
            var inputStatus = document.getElementById('flexCheckDefault');

            function cekStatus() {
                var value = inputStatus.checked
               
                if (value) {
                    var body = document.getElementsByTagName('body')[0];
                    var modal = document.querySelector('.modal');
                    var backdrop = document.querySelector('.backdrop');

                    body.classList.add('modal-open');
                    modal.classList.add('show');
                    modal.style.display = 'block';
                    backdrop.classList.add('modal-backdrop', 'fade', 'show');
                }
            }

            function closeModal() {
                var body = document.getElementsByTagName('body')[0];
                var modal = document.querySelector('.modal');
                var backdrop = document.querySelector('.backdrop');

                body.classList.remove('modal-open');
                modal.classList.remove('show');
                modal.style.display = 'none';
                backdrop.classList.remove('modal-backdrop', 'fade', 'show');
            }
        </script>
        <div class="backdrop"></div>
</body>

</html>